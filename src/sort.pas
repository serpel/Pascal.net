program raiz();
 (*Obtener la raíz cuadrada de un número real x cualquiera.*)
 
type
   alumnos = record
                 nombre: string;
                 edad: int;
             end;
 var 
   i: int;

 function ReadArr(base:int;exp:int):string;
 var
    c: int;
 begin
    c := base;
 end;

   begin

   for i := 1 to 10 do
   begin
    writeln(i*2);
   end

 end.